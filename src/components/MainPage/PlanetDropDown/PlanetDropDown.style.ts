import styled from 'styled-components';

const PlanetDropDownContainer = styled.div`
  font-weight: 400;
  color: ${(props) => props.theme.colors.textColor};
`;

export default PlanetDropDownContainer;
